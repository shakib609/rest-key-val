# Django REST Key-Value Store

## Pre-requisites

- `docker-compose`

## Running the Server Locally

```
$ docker-compose up
```

_DEBUG_ is enabled for convenience.

## Testing

```
$ docker-compose run django python3 /usr/local/app/manage.py test
```
